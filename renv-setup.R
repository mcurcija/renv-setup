# https://rstudio.github.io/renv/articles/renv.html#collaboration
# run renv::restore() in order to restore environment

library(tidyverse)
library(readxl)
library(ggdist)

# print status
renv::status()

demo(colors)